// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  USER
  ADMIN
}

model User {
  id            String       @id @default(cuid())
  name          String?
  email         String       @unique
  emailVerified DateTime?
  password      String
  avatar        Image?
  roles         Role[]       @default([USER])
  invitations   Invitation[]
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt
}

model Invitation {
  id          String   @id @default(cuid())
  email       String   @unique
  token       String   @unique
  invitedById String
  invitedBy   User     @relation(fields: [invitedById], references: [id])
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Image {
  id        String   @id @default(cuid())
  filename  String
  bucket    String?
  userId    String?  @unique
  user      User?    @relation(fields: [userId], references: [id], onDelete: Cascade)
  mixtapeId String?  @unique
  mixtape   Mixtape? @relation(fields: [mixtapeId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Dj {
  id        String   @id @default(cuid())
  name      String   @unique
  // mixtapes  DjOnMixtape[]
  userId    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// model DjOnMixtape {
//   djId      String
//   dj        Dj      @relation(fields: [djId], references: [id])
//   mixtapeId String
//   mixtape   Mixtape @relation(fields: [mixtapeId], references: [id])
//   position  Int?

//   @@id([djId, mixtapeId])
// }

model Mixtape {
  id           String   @id @default(cuid())
  name         String   @unique
  year         String
  djsAsText    String
  description  String?
  comment      String?
  tracksAsText String?
  cover        Image?
  // tracks       Track[]
  // djs          DjOnMixtape[]
  // tags         TagOnMixtape[]
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

// model Track {
//   id        String   @id @default(cuid())
//   title     String
//   artist    String?
//   startTime String?
//   position  Int?
//   mixtapeId String
//   mixtape   Mixtape  @relation(fields: [mixtapeId], references: [id])
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
// }

// model TagOnMixtape {
//   tagId     String
//   tag       Tag     @relation(fields: [tagId], references: [id])
//   mixtapeId String
//   mixtape   Mixtape @relation(fields: [mixtapeId], references: [id])

//   @@id([tagId, mixtapeId])
// }

// model Tag {
//   id        String         @id @default(cuid())
//   name      String         @unique
//   color     String?
//   mixtapes  TagOnMixtape[]
//   createdAt DateTime       @default(now())
//   updatedAt DateTime       @updatedAt
// }
